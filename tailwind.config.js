/** @type {import('tailwindcss').Config} */
const config = {
  darkMode: ['class', 'html'],
  content: ['./app/**/*.{ts,tsx}', './components/**/*.{ts,tsx}'],
  theme: {
    extend: {},
  },
  plugins: [],
};

export default config;
